// TODO: Default-export an array of two strings contrasting objects with primitives and arrays.
export default [
  "Objects hold named keys mapping to values; primitives hold a single value.",
  "Arrays are ordered lists accessed by index; objects use named keys for lookup.",
];
export default ["primitives only hold a single value, while objects use a key to map multiple",
  "Arrays are always in one order, but objects map to a value using keys"
];